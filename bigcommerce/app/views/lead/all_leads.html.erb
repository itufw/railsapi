<%= title "Leads" %>
<h3>Leads</h3>
<center><%= link_to "Create New Lead", action: 'create_leads' %></center>
<center><%= link_to "Create a lead", new_lead_path %></center>
<center><%= link_to "Link Lead to Customers", action: 'link_leads' %></center>

<%= form_tag({:controller => "lead", :action => 'all_leads'}, {:method => "get"}) do %>

<div class="form-group" style="margin-left:300px;margin-top:50px;">
	<%= render "partials/customer_filter" %>
	<%= submit_tag "Filter", class: 'btn btn-primary' %>
</div>
<%= render 'partials/paginate', objects: @leads%>
<%= render "lead_partials/leads", leads: @leads %>

<% end %>
