<%= title "Weekly Sales Dashboard" %>

<h3>Weekly Sales</h3>

<table class = "table table-striped table-bordered table-hover table-responsive">
	<tr>
		<% @sums_by_periods.each do |week_num, value| %>
			<th> <%= week_num %></th>
		<% end %>
		<th>Total</th>
	</tr>
	<tr>
		<%= render "partials/daily_sums", sums_dates_h: @sums_by_periods, dates: @sums_by_periods.keys, staff_nickname: nil, staff_id: nil %>
	<tr>
</table>